{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'echo RAILWAY_PORT=$PORT; gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8080} --log-file -'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 100
  }
}
